[project]
name = "video-downloader"
version = "1.0.0"
description = "Advanced multimedia download manager based on yt-dlp, FFmpeg, and Flet"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.13"
dependencies = [
    "flet[cli,desktop]>=0.86.5,<0.87",
    "imageio-ffmpeg>=0.5",
    "platformdirs>=4.10.0",
    "static-ffmpeg>=3.0",
    "yt-dlp[default]>=2026.7.4",
]

[dependency-groups]
dev = [
    "mypy>=2.2.0",
    "pytest>=9.1.1",
    "ruff>=0.15.21",
]

# Packaging metadata for `flet build <platform>`
[tool.flet]
org = "com.fazigondal"
product = "Video Downloader"
company = "fazigondal"
copyright = "Copyright (C) 2026 Faizan Gondal"

# Show the native window immediately in production. Hiding it makes startup
# feel cleaner, but a Python boot failure can leave the error screen hidden
# and make the desktop shortcut look like it did nothing.
[tool.flet.app]
hide_window_on_start = false
exclude = [
    ".git",
    ".github",
    ".pytest_cache",
    ".venv",
    "build",
    "dist",
    "packaging",
    "tests",
    "CHANGELOG.md",
    "CLAUDE.md",
    "CONTRIBUTING.md",
    "INSTALL.md",
    "LICENSE",
    "README.md",
    "uv.lock",
]

# Pre-compile .py -> .pyc at build time: faster cold start (no runtime
# bytecode compilation on first launch, less antivirus churn on Windows).
[tool.flet.compile]
app = true
packages = true
cleanup = true

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM"]
ignore = ["SIM117"]

[tool.mypy]
python_version = "3.13"
warn_unused_ignores = true
check_untyped_defs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
